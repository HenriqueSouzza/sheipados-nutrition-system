version: '3.4'

services:
  boilerplate-react:
    container_name: boilerplate-react
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env.development
    volumes:
      - './:/app'
    network_mode: "host"